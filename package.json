{
  "name": "at-home-penny-game",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Keep the family alive inside a 3D home: PennyGame's budgeting and care mechanics in the At Home house.",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.0",
    "three": "0.180.0"
  },
  "devDependencies": {
    "vite": "^7.3.6"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
